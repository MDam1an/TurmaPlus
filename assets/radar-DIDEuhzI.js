function e(e){let t=[],n=Date.now(),r=n+6048e5,i=e.events.filter(e=>e.categoria===`prova`&&e.data>=n&&e.data<=r);i.length>0&&t.push({id:`avaliacoes-semana`,tipo:`informativo`,texto:`📅 Existem ${i.length} avaliação${i.length>1?`ões`:``} importante${i.length>1?`s`:``} nesta semana.`});let a=e.goals.filter(e=>!e.concluida);for(let e of a){let n=e.metaTotal>0?Math.round(e.progressoAtual/e.metaTotal*100):0;n<70&&t.push({id:`meta-baixa-${e.id}`,tipo:`alerta`,texto:`⚠️ A meta "${e.titulo}" está em ${n}% — abaixo do esperado.`})}for(let n of e.subjects){let r=e.mediaPorMateria[n.id];r!==void 0&&r<n.mediaMinima&&t.push({id:`media-baixa-${n.id}`,tipo:`atencao`,texto:`📉 A média de ${n.nome} está abaixo do esperado (${r.toFixed(1)}).`})}return t.length===0&&t.push({id:`tudo-em-dia`,tipo:`positivo`,texto:`🔥 Nenhum ponto de atenção identificado agora. A turma está em dia!`}),t}function t(e){if(e.length<2)return`estavel`;let t=[...e].sort((e,t)=>e.data-t.data),n=Math.ceil(t.length/2),r=t.slice(0,n).reduce((e,t)=>e+t.nota,0)/n,i=t.slice(n).reduce((e,t)=>e+t.nota,0)/(t.length-n);return i-r>.3?`positiva`:r-i>.3?`negativa`:`estavel`}export{e as n,t};